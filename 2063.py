# median function in numpy
# how to install? type [pip install module_title] in terminal
# "/" : 단순 나눗셈 - 가장 기본적인 나눗셈 (float형)
# "//" : 나눗셈의 몫 - 나눗셈 결과의 '몫'을 가져옴
# "%" : 나눗셈의 나머지 - 나눗셈 결과의 '나머지'를 가져옴
# divmod(): 나눗셈의 몫과 나머지(튜플 형식) - 나눗셈 결과의 '몫'과 '나머지'를 한번에 가져옴

# N = int(input())
# numbers = list(map(int, input().split()))

# numbers.sort()

# m = (N-1)//2

# print(numbers[m])

divmod(5, 10)